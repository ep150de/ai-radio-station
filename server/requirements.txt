# Suno Radio server — Phase 1
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
aiofiles==24.1.0
mutagen==1.47.0          # ID3 / metadata (light, pure Python)
# ffprobe used via subprocess when needed (comes with ffmpeg in Dockerfile)

# Future / Phase 2 (commented)
# aiosqlite==0.20.0
# httpx==0.27.2          # for Piper / OpenAI calls

# Build dependency for appliance packaging (install when building):
# pyinstaller>=6.0
